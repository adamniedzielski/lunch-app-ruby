<h1>Select restaurant on your own</h1>

<%= form_for [@location, @lunch] do |f| %>
  <%= f.label :restaurant_id %> <%= f.select :restaurant_id, options_for_select(@restaurants.map{|restaurant| [restaurant.name, restaurant.id]}) %>
  <%= f.submit "Select" %>
<% end %>
